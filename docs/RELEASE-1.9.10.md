# 旁白 Aside 1.9.10

2026-09-21 · 正式图标配色

- 正式采用已选定的深紫外框＋浅乳白紫内底，保留电影图案、已确认尺寸与 PNG 透明外沿。
- 同步更新浏览器工具栏、扩展图标、页面 favicon 和 PWA 图标，刷新相关缓存版本。
- 包含此前的 Pin 状态、悬停倾斜、120ms 自动收起及重新打开回到首页的改动。
- 「查看详情」改为抽象线条图标，悬停或键盘聚焦时显示，鼠标点击后移出不再残留。
- 云同步补回「立即同步」，同时上传本地修改、拉取云端记录，并持续展示具体失败原因，避免一直显示「同步待定」。

## 更新

解压覆盖原插件目录，在 chrome://extensions 重新加载旁白，再刷新视频页面。

Chrome 应用商店更新：上传 `aside-v1.9.10-chrome-web-store.zip`（不要解压）。此专用包已移除商店不接受的 manifest `key` 字段；原 `aside-v1.9.10.zip` 仅用于本地安装。请进入现有「旁白」条目上传更新，保留原扩展身份与用户。

独立部署的数据库需具备 `supabase/002-reliable-sync.sql` 的同步结构；仅升级插件不会自动迁移数据库。

## 验证

77 项自动测试通过，安装包完成脚本语法、运行依赖、版本与 ZIP 完整性检查。已在本地交互预览检查深浅主题、窄窗口、固定与自动收起、重新打开首页等行为；本次未完成所有真实视频网站的 Chrome 扩展端到端验收。

# 旁白 Aside 1.9.7

2026-09-21 · 云同步反馈与详情入口

- 设置中补回「立即同步」按钮，失败时持续显示具体原因，区分云端需要升级、网络问题、登录过期和权限异常。
- 手动同步同时上传本地修改并拉取云端记录；即使本地列表为空，也会拉取其他设备的笔记，并提交待同步的删除记录。
- 登录同步完成后不再把「已同步」覆盖回「等待同步」。
- 详情入口改为抽象内容线条与右箭头。鼠标进入对应视频条目或键盘聚焦时出现；触屏保留直接操作入口。
- 修复鼠标点击条目后详情入口因残留焦点而常驻的问题；移出条目后隐藏，保留键盘可见焦点。
- 电影图标缩小外围透明留白，导出主体放大约 18%，保留原图案及透明背景。

## 更新

用安装包内容覆盖原插件文件夹，在 `chrome://extensions` 重新加载旁白，再刷新视频页面。

云端仍采用 `supabase/002-reliable-sync.sql` 定义的同步结构。独立部署者须在现有数据库执行此增量升级；仅升级插件不能补齐缺失的数据库字段。不要重跑建库脚本 `schema.sql`。

## 验证

72 项自动测试通过，包括数据库迁移、账号隔离、双向重试和错误反馈；安装包检查脚本语法、依赖和 ZIP 完整性。实际账号在扩展中的同步完成情况需单独验证。

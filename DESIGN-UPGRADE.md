# 旁白 Aside 1.7.4

定位：记录观看电影、访谈、视频播客时产生的想法。

## 本次更新

- 插件按钮打开可拖动的网页悬浮记录库，不挤压网页。关闭后保留窗口状态；点击网页其他位置不会自动关闭。右上角 ↗ 可主动进入完整标签页。浏览器受限页面在同一个浏览器窗口中打开标签页，不创建独立小窗。
- 悬浮球负责快速记录。记录库和快速记录互斥，切换时保存快速记录草稿；打开记录库不暂停视频，快速记录保留原有暂停/恢复逻辑。
- 首页视频卡片点击标题展开，一次展开一个；预览仅含正文、视频时间和记录标签。长正文可展开。截图、分享、编辑、删除、导出继续在详情页提供；删除整个视频移到详情页右上角。
- 使用帮助改为可随时打开/关闭的弹窗，快捷键按需展开。
- 表单改为“所属视频、记录内容、视频位置（可选）、标签（可选）”；正文优先，手动时间不要求视频当前打开。来源视频随草稿保留，切换页面不会把记录误归到另一个视频。
- 统一白色、深灰与琥珀色，增强文字对比，减少大面积斜体和空白；兼容深色和窄窗口。

## 使用新版

已安装本目录扩展：在 Chrome 的 `chrome://extensions` 找到旁白，点击重新加载，然后刷新视频页。Chrome 若提示新增的 scripting 权限，需要启用后才能使用新入口；新权限用于用户点击插件图标时在普通网页打开悬浮窗。

保留现有扩展，直接重新加载，避免卸载导致本地数据被清除。本次没有改写真实笔记或云数据库。新的安装包是 `movie-notes-v1.7.4.zip`，解压后选择其中 `movie-notes-extension` 文件夹。

浏览器内部页、扩展商店或浏览器明确禁止注入的页面，会在当前浏览器窗口打开完整记录库标签页，并显示原因。相同来源再次打开时复用该标签页。普通网页的临时故障不会再切换成独立窗口或标签页；插件图标会显示提示，悬停可查看刷新重试说明。

## 验证范围

- 39 项自动测试：原有存储隔离、并发保存、截图、同步冲突、删除与数据库事务/RLS，以及新增的窗口注入/受限页回退、消息来源检查。
- 浏览器页面回归：预览内容边界、单条展开、全文展开、时间跳转、搜索、帮助与 Esc、详情分享、表单保存和标签、浅色/深色/360px 窄窗口；原有快速记录与详情删除回归通过。
- 独立 Chrome for Testing 配置中加载真实 MV3 扩展：验证扩展 iframe 的实际存储访问、窗口拖动、入口互斥、草稿跨窗口恢复、保存到 Service Worker、嵌套弹窗 Esc、重复注入和独立页来源绑定。视频页面使用测试素材，未操作用户的浏览器配置或云账号。
- 预览图使用测试记录，位于 `design-preview/`。未逐一验证六家视频网站的线上播放器、真实 Google 登录和 Notion 导出；其原有实现保留。

1.6 的云同步数据库升级要求保持不变，参见 `TECHNICAL-UPGRADE.md`。本次未部署 PWA，也未实现后续 SaaS 平台。


## 1.7.1 体验修正与快捷键

- 普通非视频网页继续使用悬浮记录库。此版曾加入受限页面的独立小窗回退；1.7.2 已根据反馈撤销，改用同一浏览器窗口内的标签页。
- 帮助关闭图标改用居中的 SVG；查看详情仅在卡片悬停、键盘聚焦时出现，触屏保持可见；记录库橙色指示条移到导航顶部。
- 鼠标打开编辑页不自动聚焦正文；空白内容禁用保存且不显示红色错误，字数在输入框右下角；删除编辑页底部裸露的快捷键提示，保留帮助里的动作说明。
- 浏览器快捷键：Alt + Shift + N 打开/收起记录库，Alt + N 快速记录（再次触发保存），Alt + S 插入字幕。Mac 使用 Option；在使用帮助中可打开 Chrome 的快捷键设置。实际绑定从 Chrome 读取，未设置时如实显示，避免与其他扩展冲突后仍宣称可用。
- 旁白窗口内：/ 搜索、N 新建、? 帮助；输入框、输入法组合输入及弹窗中不触发单键命令。Command / Ctrl + Enter 保存，Esc 优先关闭当前弹窗。
- 验证：真实扩展的快捷键注册与命令消息路由；普通文章页注入；悬停、关闭按钮中心、导航标记、空表单、字数位置、局部快捷键和输入隔离。浏览器自动化通过命令消息验证快捷键动作，未模拟操作系统物理按键。普通文章页测试副本仅增加了测试域名权限，以模拟工具栏激活后的临时授权；发布版没有新增全站读取权限。

Chrome Commands API 参考：https://developer.chrome.com/docs/extensions/reference/api/commands

## 1.7.2 悬浮入口修正

- 默认保持当前网页内的悬浮主页，不挤压网页、不弹出独立浏览器窗口；可拖动、关闭和重新打开。
- 保留右上角“在新标签页整理记录”，作为用户主动选择的整理空间。
- 移除 `chrome.windows.create` 小窗回退，以及编辑页中的小窗关闭逻辑。
- 区分浏览器明确限制与普通运行失败：前者使用带原因说明的完整标签页，复用同一来源；后者保留当前页面并在插件图标提示重试，不再因记录正在保存或通信异常而另开窗口。
- 验证：39 项 Node/数据库测试通过；在隔离的 Chrome for Testing 中验证普通网页悬浮、关闭/重开、主动打开完整标签页、受限页同窗口标签页与复用，确认浏览器窗口数量未增加。视频测试素材验证拖动、快速记录、草稿保留、保存与来源绑定。测试未修改用户浏览器或真实笔记。

## 1.7.3 键盘操作

- 默认 Alt/Option + N 打开或聚焦快速记录，取消重复按下即保存；Alt/Option + L 打开/收起记录库。仅这两个入口注册 Chrome Commands。升级可能保留用户已有绑定，需在 Chrome 快捷键设置中改为 Alt + L；帮助页显示实际绑定。
- 快速记录及新建记录编辑器内：Alt + S 在光标处插入字幕；Alt + T 获取视频时间，时间段模式更新当前聚焦的终点，否则更新起点。新建记录编辑器增加字幕按钮，快速记录增加时间按钮。无字幕时提示并保留原文，来源视频不匹配时拒绝抓取。
- 快速记录更新时间同步刷新截图与草稿；保存/关闭等待该操作完成。恢复草稿保持原时间，不以当前播放位置覆盖。
- Command/Ctrl + Enter 保存；快速记录的焦点在按钮上时也能保存。保存成功后恢复播放器焦点，仅在打开前播放中的情况下恢复播放；失败保留编辑器并聚焦正文。
- 面板内未输入时 N 新建、S 设置、L 记录库、/ 搜索、? 帮助；输入框及输入法组合输入期间不触发单字母命令。Esc 从新建/设置/详情返回记录库，下一次收起浮层；新建记录返回前保存草稿。
- 搜索框 ↓ 进入结果，↑/↓ 选择视频，Enter 展开/收起，←/→ 收起/展开；Tab/Shift+Tab 在快速记录及悬浮主页内切换操作，避免跳到背后网页。
- 验证：39 项 Node/数据库测试通过；Chrome for Testing 的真实扩展测试覆盖重复唤起、光标处字幕、缺失字幕、时间与草稿、时间段终点、输入法隔离、按钮聚焦时保存、焦点/播放恢复、列表/设置/搜索键盘操作与原有悬浮主页行为。浏览器级快捷键验证注册与命令消息路由，编辑器内按键通过 Playwright 键盘事件验证；未测试 Windows 实机、操作系统物理按键及六个平台的线上播放器。

## 1.7.4 快速记录提示与品牌图标

- 快速记录底部直接显示保存、收起及草稿保留提示；时间、字幕按钮直接显示对应组合键，不依赖悬停提示。Mac 使用 Option / ⌘，Windows 使用 Alt / Ctrl。
- 快速记录的记录库文字入口改为旁白图标，保留键盘可访问名称，点击后保存草稿并返回旁白首页。
- 使用帮助改为“留下这一刻”“保存，随时回看”，采用用户确认的文案。
- 新图标沿用参考图的手绘黑线、柔和绿色与纸白，图形表达视频片段与旁白。原有界面操作色保持不变。图标生成原图、提示词和设计说明放在 `brand/aside-1.7.4/`；扩展图标导出为 16/32/48/128 像素。
- 验证：39 项自动测试通过；真实扩展浏览器回归覆盖原有键盘操作、草稿与保存，并验证首页图标从设置返回记录库、图标资源加载、确认文案、无悬停依赖、280/320/360 像素宽度下的布局。Mac 按键显示已验证，Windows 按键名称通过模拟平台验证，未测试 Windows 实机。

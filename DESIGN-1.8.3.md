# 旁白 1.8.3 · 玻璃表面

1.8.2 删除了背景图，但完整标签页下方因此变成均匀纯色，背景模糊几乎没有可见效果。这版为完整标签页增加低对比、无明确图案的 CSS 背光层，并让面板透出明暗差异。

- 独立主页的玻璃填充降至 48% 不透明度，保留 32px 背景模糊。
- 面板增加细颗粒与克制的边缘反光。颗粒在文字下方，不拦截点击。
- 不恢复原有几何壁纸、照片、条纹或动画。
- 嵌入网页的悬浮模式不添加背光、颗粒或内部填充，仍由网页中的外层窗口执行真实模糊。
- 标题栏、底部操作栏、导航继续保持透明，无独立模糊层。受限页面提示继续移除。
- 减少透明效果的偏好会同时关闭这些装饰并启用实色降级。

本轮已检查本地示例预览的深浅主页与浅色编辑页，确认外层 blur32、标题/操作栏透明、提示不存在、无横向溢出。未重复进行真实 MV3 浏览器全流程回归。

截图：`design-preview/v1.8.3/`。更新包：`movie-notes-v1.8.3.zip`。

重新加载扩展后，刷新旁白标签页和原视频页。修改前备份：`backups/aside-before-1.8.3.zip`。

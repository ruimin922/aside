# 旁白 1.6.0 技术优化说明

本次修改保留现有产品结构，主要修复数据可靠性。代码与隔离测试已完成；没有连接、修改或部署线上服务，也没有读取真实浏览器笔记。

## 已修改

- 悬浮笔记按账号、视频与标签页保存草稿；误点关闭、Esc、刷新后可恢复。关闭的标签页草稿可在同视频重新打开时恢复。恢复时保留原截图和时间点。
- 用跨扩展页面和后台的 Web Locks 保护本地读改写；读取失败时停止写入；请求 ID 防止快捷键连按与重试重复保存。
- 快速保存保留影片标签；明确区分“不修改标签”与“清空标签”。
- 视频归档采用平台视频标识。YouTube 不受时间点/播放列表影响；B 站分 P 分开；同名、不同 URL 不再自动合并。历史上已误合并的数据不自动拆分。
- 单条和整部删除都保留删除记录，并随离线队列重试；删除不会因旧设备上传而被恢复。
- 云端使用一个事务同步影片、条目、删除和截图；条目按自己的更新时间合并，避免修改不同条目时相互覆盖。同一条目的不同版本采用更新时间裁决，没有增加冲突编辑界面。
- 本地成功写入立即回执，不等待网络；后台每两分钟尝试重试，浏览器需要运行。云端失败不会冒充已同步。
- 同步分页读取；用服务端修改标记判断是否变化，避免设备时钟导致漏拉与反复下载未改变的截图。
- 账号、草稿、待同步数据和 Notion 配置分别保存；退出账号后显示未登录笔记库。未登录笔记不会自动上传到新账号，可退出登录查看。没有自动合并旧账号或未登录数据。
- 截图提高到最高 1280×720 / JPEG 0.82；失败时明确提示；管理页保留完整画面。现有低清截图不能凭空变清晰。
- 页面注入、侧栏与平台判断统一为六个平台；修正伪装域名匹配、页面标题的 HTML 注入与部分字幕误匹配。
- 移动查看端支持同步截图、过滤已删除条目、携带时间点返回 YouTube/B 站，并在切换账号时清除旧详情。

## 启用步骤

1. 在 Supabase 对应项目的 SQL Editor 执行 `movie-notes-extension/supabase/002-reliable-sync.sql`。已有项目不要重跑 `schema.sql`；新项目先执行 `schema.sql`，再执行升级脚本。升级脚本已在本地 PostgreSQL 环境验证，可重复执行，不删除既有笔记。
2. 更新已有扩展：在 `chrome://extensions` 对原有“旁白”点击“重新加载”，然后刷新视频页。本目录已直接更新；如果复制到另一台电脑，解压 `movie-notes-v1.6.0.zip` 并选择其中的 `movie-notes-extension` 文件夹。不要卸载原扩展来替代重新加载，否则浏览器可能清除其本地数据。
3. 将 `movie-notes-pwa` 目录更新到原移动查看端托管位置。源代码已更新，线上地址尚未发布。
4. 所有参与同步的设备升级到 1.6.0 后使用。在原账号中等待同步成功，再验证跨设备的新增、编辑、删除与截图。

云端没有升级时，1.6.0 仍可在本机记录，会保留待同步状态，并提示需要升级云端结构。旧客户端不理解新的删除记录，不应继续与新版同时写入。

## 数据与兼容边界

- 首次迁移将已有本地数据复制到升级时已登录账号；升级时未登录则复制到未登录笔记库。原始存储键仍保留为迁移恢复副本。它们是升级时的快照，不会继续更新。
- 原始代码快照在 `backups/aside-source-before-20260917.zip`，这不是用户笔记备份。不要直接退回旧代码继续记录，因为旧版不能读取新版账号分区与删除记录。
- 当前同步的是预览截图（每张最多约 2 MB 的 data URL），由现有用户行级权限保护。超限或非法图片导致该事务失败并保留本地待同步，不会静默丢图。大量原图存储与对象存储迁移不在本次范围。
- 字幕仍提取网页已有的可见文本，不包含 OCR 或语音识别。烧录字幕、跨域受限截图、受保护视频、跨域 iframe 播放器需要逐站实测；本次未宣称六个平台的所有影片都支持。
- 本次没有访问真实 Google OAuth 或 Supabase 线上数据；线上登录、实际部署权限、真实视频网站与多设备端到端验收仍待完成。

## 验证

本次结果：30 项自动化测试通过；三个隔离浏览器脚本合计 18 项检查通过，无页面运行错误。

运行 `npm ci && npm test`：包含数据回归、后台消息协议，以及基于 PGlite 的实际 PostgreSQL 迁移、事务与行级权限测试。依赖只用于开发测试，不打进扩展。

浏览器检查使用三个隔离测试脚本（浏览器是真实 Chromium，Chrome 扩展 API / 云端用受控模拟）：

- `tests/browser-regression.js`：悬浮草稿、原时间点、字幕、误关、快速重复保存和页面标题安全。
- `tests/panel-browser-regression.js`：管理页模块加载、旧数据迁移、详情展示与删除。
- `tests/pwa-browser-regression.js`：移动端列表、已删除条目过滤、截图、时间链接与账号切换。

这些脚本通过 Playwright 的代码执行工具运行。先将需测试的扩展/PWA 复制到临时目录，并仅在 `127.0.0.1:8765` 启动该目录的静态服务；其中悬浮脚本还需要临时目录根下的 `content.js`。不要把含扩展私钥的项目根目录用作静态服务根目录。

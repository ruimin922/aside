# 旁白 1.8 · 烟紫玻璃与手工引号

这版把已对齐的字体、颜色、间距落实到浏览器扩展。日常记录界面采用连续、清晰的阅读布局；手工引号用于使用帮助、空状态和分享卡片。

## 字体与层级

使用本机字体，优先已安装的 MiSans，然后是系统字体（macOS 使用 PingFang SC 等）。本版不额外下载或打包字体。

| 字号 | 用途 | 字重 / 行高 |
| --- | --- | --- |
| 24px | 页面主标题、统计数字 | 500–600 / 1.4–1.5 |
| 16px | 视频标题、记录正文、主要输入区域 | 标题 500，正文 400 / 1.5–1.85 |
| 14px | 按钮、导航、字段标题、时间戳 | 400–500 / 1.4–1.6 |
| 12px | 次要信息、字数、标签、快捷键 | 400–500 / 1.5–1.8 |

窄窗口通过换行改变布局，不缩小字号。

## 颜色

| 角色 | 深色 | 浅色 |
| --- | --- | --- |
| 页面底色 | #302A3B | #E9E4EF |
| 面板底色 | #40384D | #F5F2F8 |
| 悬停表面 | #4D445D | #E8E1EF |
| 正文 | #F6F3F8 | #352E41 |
| 次要文字 | #C6BDD0 | #655C73 |
| 主要操作 | #DDD0EF | #665278 |
| 按钮文字 | #302A3B | #FFFFFF |

首次使用默认深色；保留用户已保存的浅色/深色选择。快速记录使用烟紫色。毛玻璃只作用于窗口自身；不模糊整个网页和视频。输入区增加底色以稳定阅读，边框和紫色映光保持克制。

## 排版与间距

- 面板内边距 24px；宽屏完整主页 40px，窄屏 16–20px。
- 快速记录内边距 16px。
- 图标与文字约 8px，标题与附属信息 4px，主要分组 24px。
- 视频连续排列，以细分隔线区分；展开/悬停行出现轻背景。
- 视频行约 76px 起，长标题和窄窗口自然增高。
- 底部导航约 52px，选中线位于顶部。
- 输入框字数保留在右下角；保存操作留在编辑区域底部。

## 品牌元素

原创简化手工引号用于帮助页、空状态、分享卡片；不直接使用 Pinterest 图片。分享页与实际导出的 PNG 同步改色，记录正文优先，随后展示截图（如有）、视频来源和时间。浏览器工具栏产品图标沿用当前版本，后续单独迭代。

## 保留的交互

- 主页继续覆盖在当前网页内，支持拖动；不会另开独立的小型操作系统窗口。
- 普通列表预览只展示正文、时间、标签。截图、分享、编辑等仍在详情页。
- 快捷键、草稿恢复、时间戳与截图绑定、字幕插入保持原有规则。
- 详情入口只在鼠标悬停或键盘聚焦时显示；触屏始终可见。
- 使用帮助文案保留已确认版本。

## 验证

- 39 项数据与服务层测试通过，包含真实 PostgreSQL 模拟环境中的 RLS / 同步验证。
- 独立 Chrome 测试配置加载真实 MV3 扩展，验证悬浮主页、拖动、草稿保存/恢复、页面跳转。
- 16 组快捷键场景通过，包括中文输入法、Tab 焦点、字幕、时间戳和播放恢复。
- 280 / 360px 页面，以及同尺寸真实浏览器视口内更窄的 iframe 和快速记录窗口，无横向溢出。
- 深浅色、记录预览、详情、编辑、设置、帮助、分享与 PNG 导出已截图检查。
- 当前可见文本的计算字号检查为 24 / 16 / 14 / 12px。

截图位于 `design-preview/v1.8.0/`，全部使用独立测试数据。没有改动用户笔记或线上 PWA。

## 更新方法

已从 `movie-notes-extension/` 加载开发版的用户：在 `chrome://extensions` 点击旁白的“重新加载”，然后刷新已打开的视频页面。

通过压缩包更新：解压 `movie-notes-v1.8.0.zip`，使用其中的 `movie-notes-extension/`。保留原扩展安装，优先重新加载同一个源目录，不需要先卸载。

若此前手动选择过浅色，可点击主页左上角主题按钮切到烟紫深色。

本轮改动前的源码备份：`backups/aside-before-1.8.0.zip`。

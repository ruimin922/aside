# 旁白 1.8.1 · 磨砂玻璃修正

上一版外层紫色背景的 alpha 为 0.84，内部页面又叠加 0.42 的背景，网页只剩约 9% 透光；浅色模式透光不足 1%。因此实际观感接近实色面板。

## 材质规则

- 悬浮主页仅由网页中的外层窗口负责染色和背景模糊。内部 iframe 的 html、body、app 完全透明，不再重复遮挡。
- 深色玻璃：`rgba(27,22,39,.68)`；浅色玻璃：`rgba(248,245,252,.68)`。网页颜色随背景透入，品牌紫色作为底调。
- 背景模糊：32px；饱和度：1.25。文字保持锐利，只有窗口后面的画面被柔化。
- 快速记录使用同一材质。输入区轻叠底色，维持文字与背景的区分。
- 顶部细亮边、柔和外阴影提供窗口边界；底部导航和编辑操作栏降低填充强度。
- 完整标签页增加原创静态背景 `glass-backdrop.svg`，背景在面板下被真实模糊。它是页面自身背景，不是桌面截图。
- 深浅切换同时更新记录库和快速记录。系统要求减少透明效果或浏览器不支持模糊时，退回可读的实色面板。
- 24 / 16 / 14 / 12 字号、原有笔记与快捷键流程保持原规则。

背景采样需要透明表面，且受 backdrop root 影响：[MDN backdrop-filter](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/backdrop-filter)。所以跨 iframe 的网页模糊放在宿主页窗口上。

## 本轮已完成的验证

- JavaScript 语法检查通过。
- 39 项现有自动测试通过，覆盖存储、同步和服务层。
- 本地浏览器以生产 host CSS、生产 panel CSS 和示例数据检查浅深玻璃、快速记录与帮助弹层。
- 暖色和冷色背景的面板同区域截图，平均 RGB 差值为 13.27，证明背景确实影响面板显示。
- 细条纹经过玻璃后，按透光率归一化的边缘能量仅为原来的 0.036，验证出现实际模糊，而不只是透明染色。
- 截图和像素结果位于 `design-preview/v1.8.1/`。预览数据与用户笔记隔离。

真实 MV3 浏览器回归两次遇到自动审批超时，尚未执行。不能把本地材质预览等同于已验证本版本所有真实扩展操作。新增的 `tests/frosted-glass-browser-regression.js` 已准备好，覆盖实际宿主页、iframe、主题同步、草稿、保存和播放恢复。

## 更新

在 `chrome://extensions` 重新加载已安装的旁白，然后刷新原视频页面。新版压缩包为 `movie-notes-v1.8.1.zip`。无需卸载扩展。

改动前备份：`backups/aside-before-1.8.1.zip`。

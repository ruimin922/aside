# 旁白 1.8.4 · 首页层级

首页按「品牌 → 操作 → 记录」组织，减少重复标题和过大的宣传区。

- 「旁白」为唯一 h1，使用 24px / 600；Aside 为 12px 英文辅助标识。
- 移除独立的「灵感记录」标题，说明文字放在品牌下方，使用 12px 次级文字。
- 主题切换移到右上工具区，与帮助并列。
- 搜索与新建并排，下方显示记录统计、排序和列表；搜索输入统一为 14px。
- 窄窗口自动将搜索与新建分行，帮助缩为图标，保留键盘可访问名称。
- 保留紫色玻璃材质、现有笔记、快捷键、编辑页和详情页。

## 验证

在本地隔离示例数据中检查完整标签页深浅主题、600 / 328 / 248px 悬浮面板及较长的「记录当前视频」按钮。手动验证搜索过滤、使用帮助、Escape 返回、斜杠搜索和 N 新建；检查关键 DOM ID 无重复且仍保留。悬浮材质预览使用生产 CSS；此次没有运行实际 Chrome 扩展端到端回归。

截图：`design-preview/v1.8.4/`。更新包：`movie-notes-v1.8.4.zip`。

重新加载扩展后刷新旁白标签页与视频页。修改前备份：`backups/aside-before-1.8.4.zip`。

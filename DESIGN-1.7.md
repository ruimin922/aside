# 旁白 1.7：观看时留下的想法

范围：视频灵感记录；悬浮主页、快速记录、帮助、列表预览和编辑器。下拉预览只有正文、视频时间和标签；详情页继续保留截图、分享、编辑、删除、导出。既有笔记不改写。

## 视觉方案

- 主色：白 #FFFFFF、纸面灰 #F7F8FA、墨色 #252821、辅助灰 #656961、深琥珀 #995B13、浅琥珀 #FFF3DE。深色主题使用对应高对比色。
- 字体：系统中文无衬线承担所有操作与正文；宋体仅用于“旁白”品牌。正文 14px / 1.75，条目标题 16px / 1.5，页面标题 24px；时间使用等宽数字。
- 8px 间距网格；内容左对齐；窗口内边距 24px，小屏 16px。主要按钮实心琥珀色，其余按钮低强调。圆角 10–16px，边框明确，只有悬浮窗口有明显投影。
- 唯一突出的视觉元素是开头的“灵感记录”标题和琥珀色记录按钮。简短引导不占首屏，内容密度高于旧版。

```
┌────────────── 拖动窗口 ──────────────┐
│ 旁白 ASIDE          帮助 主题 全页 × │
│ 灵感记录                            │
│ 记录观看时产生的想法。      ＋记录   │
│ [搜索标题、内容或标签              ]│
│ 6 个视频 · 18 条记录      最近编辑 ▾│
│ ▾ 访谈标题                   查看详情│
│   YouTube · 3 条记录 · 2小时前      │
│   12:34  记录正文……                │
│          #一个标签                  │
│ ▸ 电影标题                   查看详情│
│                                    │
│ 记录库                         设置 │
└────────────────────────────────────┘

┌───────────── 新建记录 ──────────────┐
│ ‹ 返回               草稿自动保留  │
│ 所属视频 [清晰的视频标题          ]│
│ 记录内容                            │
│ [一个观点、一句台词，或你的想法。  ]│
│ [                                 ]│
│ 视频位置（可选）  时间点 / 时间段  │
│ [12:34]                  获取时间  │
│ 标签（可选）[输入后回车添加        ]│
│                       清空   保存  │
└────────────────────────────────────┘
```

## 自检与实现边界

- 保留电影用户的品牌感，清除表单里难理解的“时间刻度/观影迷思”；不使用满屏装饰、渐变大标题或大面积斜体。
- 主页与快速记录互斥；窗口隐藏时保留编辑器状态，快速记录关闭前持久化草稿。主页不改变播放状态。
- 点击卡片标题展开，详情是单独按钮；预览不加载截图，长文字按需展开，键盘可用。
- iframe 隔离宿主 CSS，内容脚本只控制窗口；扩展按钮在受限页面退回独立页面。视频操作绑定来源标签页，避免独立页把自己识别为视频。
- 不新增云平台、账户迁移或自动摘要。继续使用既有数据层和同步保障。

验收：存储/同步回归；打开、关闭、拖动、切换快速记录、草稿恢复；列表预览与详情动作；编辑保存；浅色/深色/窄屏截图。
